class Button:

	def __init__(self, text, link):
		self.text = text
		self.link = link
		
		
class Link:

	def __init__(self, label, text, link):
		self.label = label
		self.text = text
		self.link = link
		
class Label:

	def __init__(self, label, text):
		self.label = label
		self.text = text
